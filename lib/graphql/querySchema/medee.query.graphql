query paginateMedee($page:Int!) {
  paginate(sorts: [{ column: "id", order: desc }], page:$page, size:10) {
    total,
    last_page,
    ds_medee {
      id
      medee
      angilal
      ognoo
      tailbar
    }
  }
}
