query paginateMedee($page:Int!, $size:Int!) {
  paginate(sorts: [{ column: "id", order: desc }], page:$page, size:$size) {
    total,
    last_page,
    ds_medee {
      id
      medee
      angilal
      ognoo
      tailbar
    }
  }
}
